#include <ansi.h>
inherit F_CLEAN_UP;
int hhh(int a);
int main(object me, string arg)
{
	if(arg=="add")
	   DAILY_D->quest(me);
   else
	   DAILY_D->set_shanchu(me);
   if(arg=="add")
	   DAILY_D->quest(me);
}